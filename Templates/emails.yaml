id: emails
info:
  name: Emails
  author: Pegasus
  severity: info
  description: Extracts email

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Z|a-z]{2,}$"
