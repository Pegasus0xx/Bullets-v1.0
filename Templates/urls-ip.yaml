id: urls-ip
info:
  name: URLs ip
  author: Pegasus
  severity: info
  description: Extracts URLs
file:
  - extensions:
      - all
    extractors:
      - type: regex
        regex:
          - "(https?://(?:\\d{1,3}\\.){3}\\d{1,3}(?::\\d+)?(?:/[\\S]*)?)"